
export interface IPossible{
    id?: string;
    name:string;
}

export interface IPossibleItem{
    id?: string;
    image:string;
    name:string;
    descr:string;
    isPossible:boolean;
    possibleId:string;
}